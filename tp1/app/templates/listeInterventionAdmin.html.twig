{% extends 'base_auth.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
    <h1>Interventions en cours</h1>
    <table>
        <thead>
            <tr>
                <th>Description</th>
                <th>intervenant</th>
                <th>Standardiste</th>
                <th>Client</th>
                <th>Date</th>
                <th>Statut</th>
                <th>Degré</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
            {% for allIntervention in allInterventions %}
                <tr>
                    <td>{{ allIntervention.description}}</td>
                    <td>{{ allIntervention.intervenants }}</td>
                    <td>{{ allIntervention.nomStandardiste }} {{ allIntervention.prenomStandardiste }}</td>
                    <td>{{ allIntervention.nomClient }} {{ allIntervention.prenomClient }}</td>
                    <td>{{ allIntervention.date }}</td>
                    <td>{{ allIntervention.statutIntervention}}</td>
                    <td>{{ allIntervention.degreIntervention }}</td>
                    <td>
                    <a href="addCommentaire.php?id={{ intervention.id_intervention}}"><i class="bi bi-chat-dots"></i></a> 
                    <a href="addEdit.php?id={{ intervention.id_intervention}}"><i class="bi bi-eye-fill"></i></a> 
                    
                    </td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
